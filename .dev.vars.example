# Empty declaration used by `bun run types` for deterministic Worker Secret types.
# For real Access testing, copy to ignored .dev.vars and set a read-only PAT there.
# `bun run dev` uses synthetic GitHub data and does not need this file.
# Production uses `wrangler secret put GITHUB_TOKEN`; this example is never deployed.
GITHUB_TOKEN=
